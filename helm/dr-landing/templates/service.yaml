apiVersion: v1
kind: Service
metadata:
  name: {{ include "dr-landing.fullname" . }}
  labels:
    {{- include "dr-landing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  selector:
    {{- include "dr-landing.selectorLabels" . | nindent 4 }}
